 <% if book.favorited_by?(current_user) %>
    <%= link_to store.favorites.count, store_favorites_path(store), method: :delete , remote: true, class: "fas fa-heart heart_red" %>
  <% else %>
    <%= link_to book.favorites.count, book_favorites_path(book), method: :post , remote: true, class: "fas fa-heart heart_blue" %>
  <% end %>


